<div class="container">
  <div class="wishlist-title mb-3">
      <h1>Your Wishlist</h1>
  </div>
  <% if @wishlists.empty? %>
    <p>No games added to wishlists yet</p>
  <% else %>
    <% @wishlists.each do |wishlist| %>
      <%= render partial: 'wishlist-card', locals: {wishlist: wishlist} %>
    <% end %>
  <% end %>
</div>
