<h1>Your Wishlist</h1>

<% if @wishlists.empty? %>
<p>No games added to wishlists yet</p>
<% else %>
<div class="container">
    <% i = 0 %>
    <% @wishlists.each do |wishlist| %>
      <%= render partial: 'wishlist-card', locals: {wishlist: wishlist, i: i} %>
    <% end %>
</div>
<% end %>
